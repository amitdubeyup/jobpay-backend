name: 'JobPay Backend CodeQL Config'

disable-default-queries: false

queries:
  - name: security-and-quality
    uses: security-and-quality
  - name: security-extended
    uses: security-extended

paths-ignore:
  - node_modules
  - dist
  - coverage
  - '**/*.spec.ts'
  - '**/*.test.ts'
  - 'test/**'

paths:
  - src

packs:
  - codeql/javascript-queries
